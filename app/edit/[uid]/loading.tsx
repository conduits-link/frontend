import Loading from "@/components/wrappers/Loading";

export default function LoadingEdit() {
	return (
		<div style={{ height: "100vh" }}>
			<Loading>Warming up the burners...</Loading>
		</div>
	);
}
